{% extends 'base.html.twig' %}

{% block title %}{{title}}{% endblock %}

{% block body %}
                {% for type,messages in app.flashes(["success","danger","info"])%}
                    {% for message in messages %}
                        <div class="alert alert-{{type}}" style={"background-color": "#cd5300";"width": "100%";"text-align": "center";}>{{message}}</div>
                    {% endfor %}
                 {% endfor %}
                <div class="text-center m-3 p-3">
                <h3 class="text-center">Modification du mot de passe:</h3>
                        {{form(form)}}
                 </div>
                <div class="text-center">
                      <a class="text-danger" href={{path("app_moncompte")}}>Retourner à mon commpte</a>
                </div>
{% endblock %}
